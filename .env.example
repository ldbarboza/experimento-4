# Environment variables for experimento-4
# Copy this file to .env.local and fill in the values as needed.
# Currently no environment variables are required (in-memory store, no external DB).

# Example (for future use):
# DATABASE_URL=
# NEXTAUTH_SECRET=
