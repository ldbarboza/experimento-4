# Environment variables for experimento-4
# Copy this file to .env.local and fill in the values

# No external services are required for the in-memory store.
# Add future environment variables here (e.g., database URLs, API keys).

# NEXT_PUBLIC_APP_URL=http://localhost:3000
